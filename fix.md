je voudrai trouver une solution pour les api tier utilisée, 
les actions sur api 
- fetch car price pour recuperer des les carte de manière unitaire (skryfall mais je voudrai utiliser une autre api)
- fetch set pour recupérer la liste des sets 
- fetch set card pour recupérer les carte d'un set 
- fetch more card pour recuperer la suite des fetch set card on boucle et on recupére tous. 

Dans l'idée je voudrai proposer une 

